import * as React from "react";
import { render } from "react-dom";
import SocialFlow from "./SocialFlow";

const footer = () => {
    return ( 
        <div>
            Derrick Xu's Home page
            <SocialFlow />
        </div>

     );
}
 
export default footer;